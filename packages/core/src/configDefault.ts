export const configDefault = {
  /* [text] whether to restrict the number of enterable characters to maxLength,
  if specified in the JSON schema */
  restrict: true,

  /* [text] whether to resize the input's width to maxLength,
  if specified in the JSON schema */
  trim: true
};
