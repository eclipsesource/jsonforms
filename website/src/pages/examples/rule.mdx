---
name: Rule
route: /examples/rule
---

import Rule from '../../components/examples/Rule';

# Rule

This example uses a [Rule](/docs/uischema/rules) that controls the enablement of a control if the 'Is Alive' checkbox is unchecked.
Give it a try!

<Rule />
