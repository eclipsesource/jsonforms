---
name: Control
route: /examples/control
---

import { 
  Control, 
  ExtendedControl 
} from '../../components/examples/Control'

# Control

These are the default controls JSON Form provides:

<Control />

Controls can be configured via `options`, see the following examples:

<ExtendedControl />
