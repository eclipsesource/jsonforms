---
id: control
title: Control
---

import {
  Control,
  ExtendedControl
} from '../../src/components/examples/Control'

These are the default controls JSON Form provides:

<Control />

Controls can be configured via `options`, see the following examples:

<ExtendedControl />

To see the how the `options` parameter is used, have a look at the `UI Schema` tab in the demo above.
Available `options` can be found [here](/docs/uischema/controls#options).
