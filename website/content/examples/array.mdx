---
id: array
title: Array Example
description: The array renderer is used, when an object with type `array` is used
sidebar_label: Array
---

import { Array } from '../../src/components/examples/Array';

The array renderer is used, when an object with type `array` is used.

To get a good overview, have a look at the `Schema` tab in the demo below.

<Array />
